[pytest]
# Skip problematic tests when running full test suite 
# Comment these out when you want to run specific tests directly
addopts = --skip-ui-tests

# Define custom markers
markers =
    ui_test: mark tests that interact with Qt UI components
    slow: mark tests that are slow to execute
    integration: mark integration tests
    memory_intensive: mark tests that use a lot of memory 